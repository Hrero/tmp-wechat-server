const defaultOptions = {
    creatTime: {
        type: Number,
        default: new Date().getTime()
    },
    updataTime: {
        type: Number,
        default: new Date().getTime()
    }
}
module.exports = defaultOptions